<%= form_with(model: @todo, local: true) do |f| %>

  <table class="table table-hover">
    <tbody>
      <tr>
        <th scope="row"><%= f.label :description, "Descripción" %></th>
        <td><%= f.text_field :description %> </td>
      </tr>
    </tbody>
  </table>

    <p></p>
    <%= f.submit "Ingresar", class: 'btn btn-info' %>
<% end %>
